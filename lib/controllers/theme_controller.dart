import 'package:get/get.dart';
import '../themes/theme_service.dart';

class ThemeController extends GetxController {
  // final themeService = ThemeService();

  // RxBool get isDarkMode => themeService.isDark;

  void toggleTheme() {
    // themeService.toggleTheme();
  }
}
